# =============================================================================
# ENVIRONMENT CONFIGURATION TEMPLATE (.env.example)
# =============================================================================
# Copy this file to .env.local for local development.
# WARNING: NEVER commit .env, .env.local or real secrets to version control.
# =============================================================================

# Application Environment
VITE_APP_ENV=development
VITE_APP_TITLE="Criptografía Interactiva"

# Optional Future Backend / API Configuration (For Full-Stack Mode)
# VITE_API_BASE_URL=http://localhost:3000/api/v1
# VITE_ENABLE_REMOTE_EVALUATION=false
# VITE_TELEMETRY_ENABLED=false
